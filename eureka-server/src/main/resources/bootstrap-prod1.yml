server:
  port: 8101

spring:
  application:
    name: eureka-node-1


eureka:
  instance:
    hostname: eureka-node-1
  client:
    serviceUrl:
      defaultZone: http://eureka-node-2:${server.port}/eureka/
