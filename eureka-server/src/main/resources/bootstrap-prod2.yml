server:
  port: 8101

spring:
  application:
    name: eureka-node-2


eureka:
  instance:
    hostname: eureka-node-2
  client:
    serviceUrl:
      defaultZone: http://eureka-node-1:${server.port}/eureka/
